en:
  layouts:
    application:
      base_title: <%= app_name.titleize %>
    header:
      <%= app_name %>: <%= app_name.titleize %>
      home: Home
      signin: Sign In
      signout: Sign Out
      register: Register
    footer:
      author: ! 'Author: <%= (ENV['USER'] || ENV['USERNAME'] || "Your name here").capitalize %>'